server:
  port: 8515

ali:
  address: 47.103.107.73

spring:
  application:
    name: service-pay
  cloud:
    nacos:
      discovery:
        server-addr: ${ali.address}:8848
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${ali.address}:3306/db_spzx?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=true
    username: root
    password: 1234
  data:
    redis:
      host: ${ali.address}
      port: 6379
  config:
    import: application-alipay.yml

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*/*.xml